test_interfaces_terse:
  - command: show interfaces terse lo0
  - item:
    id: ./name
    xpath: //physical-interface[normalize-space(name) = "lo0”]
    tests:
      - is-equal:admin-status,up
        info:"Test Succeeded”
        err:"Test Failed!!"
